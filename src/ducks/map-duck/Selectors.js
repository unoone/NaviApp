export const selsectVisibleStatus = state => state.map.placemarkVisible;

export const selsectPlacemarkList = state => state.map.placeMarkList;

export const selectCoordinates = state => state.map.coordinates;
